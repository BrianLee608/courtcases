/** @jsx React.DOM */
"use strict";
var React = require('react');

var ReactApp = React.createClass({
      render: function () {
        return (
          <h1>Hello</h1>
        )
      }
  });

module.exports = ReactApp;
